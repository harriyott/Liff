extends layout

block content
  header.home-top
    h1.entry-title= title
    nav
      a.search-link(href='#') Search
      a.random-link(href='#') Random
    form.search-form
      input.search-text(type='search',name='s')
      button.search-submit